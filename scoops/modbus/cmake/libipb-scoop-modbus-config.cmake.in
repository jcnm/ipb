@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(ipb-common REQUIRED)
find_dependency(PkgConfig REQUIRED)
pkg_check_modules(LIBMODBUS REQUIRED libmodbus)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/libipb-scoop-modbus-targets.cmake")

check_required_components(libipb-scoop-modbus)
